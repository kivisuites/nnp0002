{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'DB URL is set: ' && [ -z \"$DATABASE_URL\" ] && echo 'NO' || echo 'YES' && sleep 5 && (npx prisma migrate deploy || npx prisma db push --accept-data-loss) && npx prisma db seed && npm run start:prod",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
